import { describe, it, expect } from 'vitest';

/**
 * S1 dummy test — verifies vitest setup works.
 * S2/S3 will add real tests for utils/phq9 + utils/date.
 */
describe('vitest setup', () => {
  it('runs', () => {
    expect(1 + 1).toBe(2);
  });
});
